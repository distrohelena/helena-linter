import { earlyReturnRule } from "./rules/early-return-rule.js";

/**
 * Exposes the public early-return rule entrypoint.
 */
export { earlyReturnRule };

/**
 * Exposes the public early-return rule as the default subpath export.
 */
export default earlyReturnRule;
