import { redundantElseIfRule } from "./rules/redundant-else-if-rule.js";

/**
 * Exposes the public redundant-else-if rule entrypoint.
 */
export { redundantElseIfRule };

/**
 * Exposes the public redundant-else-if rule as the default subpath export.
 */
export default redundantElseIfRule;
