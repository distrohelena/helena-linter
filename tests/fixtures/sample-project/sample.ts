export class Sample {
    run(flag: boolean): string[] {
        const first = "a";
        const second = "b";

        if (!flag) {
            return [first, second];
        }
        return [first, second];
    }
}
