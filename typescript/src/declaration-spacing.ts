import { declarationSpacingRule } from "./rules/declaration-spacing-rule.js";

/**
 * Exposes the public declaration-spacing rule entrypoint.
 */
export { declarationSpacingRule };

/**
 * Exposes the public declaration-spacing rule as the default subpath export.
 */
export default declarationSpacingRule;
