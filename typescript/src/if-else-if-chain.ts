import { ifElseIfChainRule } from "./rules/if-else-if-chain-rule.js";

/**
 * Exposes the public if-else-if-chain rule entrypoint.
 */
export { ifElseIfChainRule };

/**
 * Exposes the public if-else-if-chain rule as the default subpath export.
 */
export default ifElseIfChainRule;
