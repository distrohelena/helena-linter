import { ifFollowingSpacingRule } from "./rules/if-following-spacing-rule.js";

/**
 * Exposes the public if-following-spacing rule entrypoint.
 */
export { ifFollowingSpacingRule };

/**
 * Exposes the public if-following-spacing rule as the default subpath export.
 */
export default ifFollowingSpacingRule;
